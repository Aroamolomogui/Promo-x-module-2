"use strict";

// las variables sirven para guardar cosas tienen un valor guardo información le asigno un valor le llamo de una forma genérica lo uqe yo quiera le puedo asignar un valor cero osea nulo, boolean son true o false string son cadenas de texto.

// para hacer una variable lo 1º es declararla --->

// let cambia - nombre que me de la gana que tenga que ver con lo que guarda
// const no cambia
let officeAdress; //declarada
console.log("quiero saber la calle " + officeAdress);
officeAdress = "calle de los hnos pinzón"; //valor metido
console.log("quiero saber la calle " + officeAdress);

//esta variable cuando hago el console log me da directamente el valor pepe porque la he declarado y asignado valor del tiri//
let nombre = "pepe";
console.log(nombre);
let campoBusqeuda;

// una variable sirve también para qu eme guarde por ejemplo un dto linkado de un archivo al otro que no depende de mi, si no que seguramente introducirá le usuario como por ejemplo cuando buscas en una barra de navegación ese dato se guarda en algún dato que es esa variable

let earnings = 12020;
let expenses = 5342;
let benefits = earnings - expenses;
console.log("cuanto es el beneficio " + benefits);

// Ejercicio variable error en la dirección

const myaddress = "Móstoles";
myaddress = "Fuenlabrada";

//esto da error por que no es una variable es una constante
